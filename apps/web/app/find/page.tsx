
import QueryForm from "@repo/ui/query";

// put the name and image and user id in seprate so the {} thse wotn show up in the button
export default function AddFriends() {
  
  return (
    <>
  <QueryForm></QueryForm>
    </>
  );
}
